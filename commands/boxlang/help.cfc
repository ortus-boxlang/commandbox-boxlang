component excludeFromHelp=true {

	function run(){
		print.line();
		print.yellow( "The " );
		print.boldYellow( "BoxLang CLI" );
		print.yellowLine( " namespace is designed to help developers easily build and maintain BoxLang Applications." );
		print.yellowLine( "Use these commands to start servers, interact with BoxLang and much more." );
		print.yellowLine(
			"Type help before any command name to get additional information on how to call that specific command."
		);
		print.line();
	}

}
